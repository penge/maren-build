const build = require('./src/build/index.js');
const buildFile = require('./src/build-file/index.js');
const buildFilePath = require('./src/build-file-path/index.js');

module.exports = {
  build,
  buildFile,
  buildFilePath
};
